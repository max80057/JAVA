題目:
有一個機器人從(0, 0)2D 平面上的原點位置開始。給定一系列動作，判斷這個機器人是否在完成動作後結束。(0, 0)

給你一個字符串moves，代表機器人的移動順序，其中moves[i]代表它的移動。有效的移動是（右）、（左）、（上）和（下）。ith'R''L''U''D'

true如果機器人在完成所有移動後返回原點，則返回，否則false返回。

注意：機器人“面對”的方式無關緊要。'R'總是讓機器人向右移動一次，'L'總是讓它向左移動，等等。另外，假設機器人的移動幅度對於每次移動都是相同的。

程式思路:
一開始想說用一個容器，將相反方向的去除，最後檢查有沒有內容物。
參考別人解法以後發現，因為是單純的方向，可以直接簡單的用數字替代，方向就用+-替代。




示例 1：

輸入： moves = "UD"
輸出： true
解釋：機器人向上移動一次，然後向下移動一次。所有的動作都有相同的幅度，所以它最終在它開始的原點。因此，我們返回 true。
示例 2：

輸入： moves = "LL"
輸出： false
解釋：機器人向左移動兩次。它在原點左側結束了兩個“移動”。我們返回 false 因為它在移動結束時不在原點。
